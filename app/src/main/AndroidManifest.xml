<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
          package="com.zwc.sellsys.android">

    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE"/>
    <uses-permission android:name="android.permission.ACCESS_WIFI_STATE"/>
    <uses-permission android:name="android.permission.READ_PHONE_STATE"/>
    <uses-permission android:name="android.permission.INTERNET"/>

    <application
        android:name=".App"
        android:allowBackup="true"
        android:fullBackupContent="true"
        android:icon="@drawable/ic_apple"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <meta-data
            android:name="UMENG_APPKEY"
            android:value="5c19e572f1f556849100007d"/>
        <meta-data
            android:name="UMENG_CHANNEL"
            android:value="Umeng"/>

        <!-- tencent bugly -->
        <!-- 配置APP渠道号 -->
        <meta-data
            android:name="BUGLY_APP_CHANNEL"
            android:value="bugly"/>

        <meta-data
            android:name="YOUMI_CHANNEL"
            android:value="90">
        </meta-data>

        <activity android:name=".activity.UserInfoActivity">
        </activity>
        <activity android:name=".activity.AddUserActivity"/>
        <activity android:name=".activity.MainFragmentActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN"/>
                <action android:name="android.intent.action.VIEW"/>

                <category android:name="android.intent.category.LAUNCHER"/>
            </intent-filter>
        </activity>
        <activity android:name=".activity.SpecificationActivity">
        </activity>
        <activity android:name=".activity.OrderInfoActivity">
        </activity>
        <activity android:name=".activity.AddOrderInfoActivity">
        </activity>
        <activity android:name=".activity.ChooseUserActivity">
        </activity>
        <activity android:name=".activity.ChooseSpecificationActivity">
        </activity>
        <activity android:name=".activity.AddSpecificationActivity">
        </activity>
        <activity android:name=".activity.CompensationInfoActivity">
        </activity>
        <activity android:name=".activity.SplashActivity">
        </activity>
    </application>

</manifest>